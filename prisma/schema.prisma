generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model appointment_history {
  appointment_id       String              @unique(map: "uq_appointment_id") @db.VarChar(20)
  patient_id           String              @db.VarChar(20)
  doctor_name          String              @db.VarChar(100)
  department           String?             @db.VarChar(100)
  appointment_date     DateTime            @db.Date
  appointment_time     DateTime            @db.Time(6)
  token_number         Int?
  booking_source       String?             @db.VarChar(30)
  appointment_status   String?             @db.VarChar(30)
  chief_complaint      String?
  symptoms             String?
  lab_test_details     String?
  scan_details         String?
  consultation_fee     Decimal?            @db.Decimal(10, 2)
  bill_amount          Decimal?            @db.Decimal(10, 2)
  payment_status       String?             @db.VarChar(20)
  payment_mode         String?             @db.VarChar(30)
  checkin_time         DateTime?           @db.Timestamp(6)
  checkout_time        DateTime?           @db.Timestamp(6)
  id                   BigInt              @id @default(autoincrement())
  branch_id            String?             @db.VarChar(10)
  department_id        String?             @db.VarChar(20)
  user_id              String?             @db.VarChar(20)
  assigned_doctor      String?             @db.VarChar(50)
  reason_for_visit     String?             @db.VarChar(500)
  referred_by          String?             @db.VarChar(100)
  employee_id          String?             @db.VarChar(20)
  created_by           String?             @db.VarChar
  created_at           DateTime?           @default(now()) @db.Timestamp(6)
  status               String?             @db.VarChar
  schedule_id          BigInt?
  diagnosis_id         String?             @db.VarChar(20)
  diagnosis_text       String?
  prescription_details String?
  cancel_reason        String?             @db.VarChar
  employees            employees?          @relation(fields: [employee_id], references: [employee_id], onDelete: Cascade)
  doctor_schedule      doctor_schedule?    @relation(fields: [schedule_id], references: [schedule_id], onDelete: NoAction, onUpdate: NoAction)
  branch               branch?             @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_appointment_branch")
  department_master    department_master?  @relation(fields: [department_id], references: [department_id], onDelete: Cascade, map: "fk_appointment_department")
  patient_bio_data     patient_bio_data    @relation(fields: [patient_id], references: [patient_id], onDelete: Cascade, map: "fk_appointment_patient")
  user_table           user_table?         @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_appointment_user")
  chemotherapy_plan    chemotherapy_plan[]
  encounter            encounter?
  lab_history          lab_history[]
  lab_order            lab_order[]
  patient_history      patient_history[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model branch {
  branch_id           String                @unique(map: "uq_branch_branch_id") @default(dbgenerated("nextval('branch_branch_id_seq'::regclass)")) @db.VarChar(10)
  branch_type         String                @db.VarChar(20)
  district            String?               @db.VarChar(100)
  state_name          String?               @db.VarChar(100)
  branch_code         String?               @db.VarChar(20)
  country             String?               @db.VarChar(50)
  total_beds          Int?
  branch_status       String?               @default("Active") @db.VarChar(20)
  date_of_establish   DateTime?             @db.Date
  medical_services    String?               @db.VarChar(100)
  id                  BigInt                @id @default(autoincrement())
  branch_name         String?               @db.VarChar(50)
  branch_area         String?               @db.VarChar(50)
  address             String?               @db.VarChar(500)
  emergency_no        String?               @db.VarChar(20)
  hospital_id         String?               @db.VarChar(20)
  branch_license_no   String?               @db.VarChar(20)
  website_address     String?               @db.VarChar(100)
  pan_no              String?               @db.VarChar(20)
  gst_no              String?               @db.VarChar(20)
  total_no_emp        String?               @db.VarChar(500)
  branch_email        String?               @db.VarChar(100)
  fax_no              String?               @db.VarChar(20)
  branch_pincode      Int?
  branch_photo_url    String?               @db.VarChar
  appointment_history appointment_history[]
  hospital            hospital?             @relation(fields: [hospital_id], references: [hospital_id], onDelete: Cascade, map: "fk_branch_hospital")
  chemotherapy_master chemotherapy_master[]
  chemotherapy_plan   chemotherapy_plan[]
  doctor_schedule     doctor_schedule[]
  employees           employees[]
  encounter           encounter[]
  lab_history         lab_history[]
  lab_order           lab_order[]
  lab_order_item      lab_order_item[]
  lab_report          lab_report[]
  lab_result          lab_result[]
  lab_test_category   lab_test_category[]
  lab_test_master     lab_test_master[]
  patient_bio_data    patient_bio_data[]
  patient_history     patient_history[]
  prescription        prescription[]
  sample_collection   sample_collection[]
  user_branch_mapping user_branch_mapping[]
  user_log            user_log[]
  user_table          user_table[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model chemotherapy_administration {
  id                                                               BigInt                  @id @default(autoincrement())
  administration_id                                                String                  @unique(map: "uq_chemo_administration") @db.VarChar(20)
  chemotherapy_cycle_id                                            String                  @db.VarChar(20)
  chemotherapy_plan_item_id                                        String                  @db.VarChar(20)
  administration_date                                              DateTime                @db.Date
  administration_start_time                                        DateTime?               @db.Timestamp(6)
  administration_end_time                                          DateTime?               @db.Timestamp(6)
  administered_dose                                                Decimal?                @db.Decimal(10, 2)
  administered_dose_unit                                           String?                 @db.VarChar(30)
  administration_route                                             String?                 @db.VarChar(50)
  infusion_rate                                                    String?                 @db.VarChar(100)
  infusion_duration_minutes                                        Int?
  infusion_completed                                               Boolean?                @default(false)
  administered_by                                                  String?                 @db.VarChar(20)
  verified_by                                                      String?                 @db.VarChar(20)
  iv_site                                                          String?                 @db.VarChar(100)
  iv_access_type                                                   String?                 @db.VarChar(100)
  cannula_size                                                     String?                 @db.VarChar(50)
  peripheral_line                                                  Boolean?                @default(false)
  central_line                                                     Boolean?                @default(false)
  picc_line                                                        Boolean?                @default(false)
  port_used                                                        Boolean?                @default(false)
  pump_used                                                        Boolean?                @default(false)
  pump_serial_no                                                   String?                 @db.VarChar(100)
  oxygen_support                                                   Boolean?                @default(false)
  steroid_given                                                    Boolean?                @default(false)
  antihistamine_given                                              Boolean?                @default(false)
  antiemetic_given                                                 Boolean?                @default(false)
  hydration_given                                                  Boolean?                @default(false)
  emergency_medication_given                                       Boolean?                @default(false)
  treatment_stopped                                                Boolean?                @default(false)
  interruption_reason                                              String?
  doctor_informed                                                  Boolean?                @default(false)
  nursing_notes                                                    String?
  administration_status                                            String?                 @default("Completed") @db.VarChar(30)
  remarks                                                          String?
  active_status                                                    Int?                    @default(1) @db.SmallInt
  deleted_flag                                                     Boolean?                @default(false)
  created_by                                                       String?                 @db.VarChar(20)
  created_at                                                       DateTime?               @default(now()) @db.Timestamp(6)
  updated_by                                                       String?                 @db.VarChar(20)
  updated_at                                                       DateTime?               @default(now()) @db.Timestamp(6)
  chemotherapy_cycle                                               chemotherapy_cycle      @relation(fields: [chemotherapy_cycle_id], references: [chemotherapy_cycle_id], onDelete: Cascade, map: "fk_chemo_admin_cycle")
  employees_chemotherapy_administration_verified_byToemployees     employees?              @relation("chemotherapy_administration_verified_byToemployees", fields: [verified_by], references: [employee_id], map: "fk_chemo_admin_doctor")
  employees_chemotherapy_administration_administered_byToemployees employees?              @relation("chemotherapy_administration_administered_byToemployees", fields: [administered_by], references: [employee_id], map: "fk_chemo_admin_nurse")
  chemotherapy_plan_items                                          chemotherapy_plan_items @relation(fields: [chemotherapy_plan_item_id], references: [chemotherapy_plan_item_id], map: "fk_chemo_admin_plan_item")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model chemotherapy_adverse_event {
  id                                                           BigInt             @id @default(autoincrement())
  adverse_event_id                                             String             @unique(map: "uq_chemo_adverse_event") @db.VarChar(20)
  chemotherapy_cycle_id                                        String             @db.VarChar(20)
  event_date                                                   DateTime           @default(now()) @db.Timestamp(6)
  adverse_event_name                                           String             @db.VarChar(200)
  adverse_event_category                                       String?            @db.VarChar(100)
  nausea                                                       Boolean?           @default(false)
  vomiting                                                     Boolean?           @default(false)
  diarrhea                                                     Boolean?           @default(false)
  constipation                                                 Boolean?           @default(false)
  mucositis                                                    Boolean?           @default(false)
  fever                                                        Boolean?           @default(false)
  fatigue                                                      Boolean?           @default(false)
  neuropathy                                                   Boolean?           @default(false)
  alopecia                                                     Boolean?           @default(false)
  skin_rash                                                    Boolean?           @default(false)
  anemia                                                       Boolean?           @default(false)
  neutropenia                                                  Boolean?           @default(false)
  thrombocytopenia                                             Boolean?           @default(false)
  infection                                                    Boolean?           @default(false)
  bleeding                                                     Boolean?           @default(false)
  pain                                                         Boolean?           @default(false)
  reaction_grade                                               String?            @db.VarChar(30)
  ctcae_grade                                                  String?            @db.VarChar(30)
  severity                                                     String?            @db.VarChar(30)
  reaction_description                                         String?
  treatment_interrupted                                        Boolean?           @default(false)
  treatment_stopped                                            Boolean?           @default(false)
  hospitalization_required                                     Boolean?           @default(false)
  icu_required                                                 Boolean?           @default(false)
  emergency_medication_given                                   Boolean?           @default(false)
  medication_given                                             String?
  dose_modified                                                Boolean?           @default(false)
  dose_reduced                                                 Boolean?           @default(false)
  reduction_percentage                                         Decimal?           @db.Decimal(5, 2)
  dose_delayed                                                 Boolean?           @default(false)
  delay_days                                                   Int?
  doctor_action                                                String?
  nursing_action                                               String?
  physician_id                                                 String?            @db.VarChar(20)
  reported_by                                                  String?            @db.VarChar(20)
  resolved                                                     Boolean?           @default(false)
  resolution_date                                              DateTime?          @db.Date
  remarks                                                      String?
  active_status                                                Int?               @default(1) @db.SmallInt
  deleted_flag                                                 Boolean?           @default(false)
  created_by                                                   String?            @db.VarChar(20)
  created_at                                                   DateTime?          @default(now()) @db.Timestamp(6)
  updated_by                                                   String?            @db.VarChar(20)
  updated_at                                                   DateTime?          @default(now()) @db.Timestamp(6)
  chemotherapy_cycle                                           chemotherapy_cycle @relation(fields: [chemotherapy_cycle_id], references: [chemotherapy_cycle_id], onDelete: Cascade, map: "fk_chemo_adverse_cycle")
  employees_chemotherapy_adverse_event_physician_idToemployees employees?         @relation("chemotherapy_adverse_event_physician_idToemployees", fields: [physician_id], references: [employee_id], map: "fk_chemo_adverse_doctor")
  employees_chemotherapy_adverse_event_reported_byToemployees  employees?         @relation("chemotherapy_adverse_event_reported_byToemployees", fields: [reported_by], references: [employee_id], map: "fk_chemo_adverse_reported_by")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model chemotherapy_cycle {
  id                          BigInt                        @id @default(autoincrement())
  chemotherapy_cycle_id       String                        @unique(map: "uq_chemotherapy_cycle_id") @db.VarChar(20)
  chemotherapy_plan_id        String                        @db.VarChar(20)
  cycle_number                Int
  cycle_day                   Int?
  planned_date                DateTime                      @db.Date
  actual_date                 DateTime?                     @db.Date
  next_cycle_date             DateTime?                     @db.Date
  cycle_interval_days         Int?
  cycle_status                String?                       @default("PLANNED") @db.VarChar(30)
  completion_status           String?                       @default("PENDING") @db.VarChar(30)
  treatment_delay             Boolean?                      @default(false)
  delay_days                  Int?
  delay_reason                String?
  rescheduled_date            DateTime?                     @db.Date
  physician_approved          Boolean?                      @default(false)
  approval_date               DateTime?                     @db.Timestamp(6)
  cycle_completed             Boolean?                      @default(false)
  completion_date             DateTime?                     @db.Timestamp(6)
  cancellation_reason         String?
  remarks                     String?
  active_status               Int?                          @default(1) @db.SmallInt
  deleted_flag                Boolean?                      @default(false)
  created_by                  String?                       @db.VarChar(20)
  created_at                  DateTime?                     @default(now()) @db.Timestamp(6)
  updated_by                  String?                       @db.VarChar(20)
  updated_at                  DateTime?                     @default(now()) @db.Timestamp(6)
  chemotherapy_administration chemotherapy_administration[]
  chemotherapy_adverse_event  chemotherapy_adverse_event[]
  chemotherapy_plan           chemotherapy_plan             @relation(fields: [chemotherapy_plan_id], references: [chemotherapy_plan_id], onDelete: Cascade, map: "fk_chemo_cycle_plan")
  chemotherapy_followup       chemotherapy_followup[]
  chemotherapy_lab_review     chemotherapy_lab_review[]
  chemotherapy_vitals         chemotherapy_vitals[]

  @@unique([chemotherapy_plan_id, cycle_number], map: "uq_plan_cycle")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model chemotherapy_followup {
  id                    BigInt             @id @default(autoincrement())
  followup_id           String             @unique(map: "uq_chemotherapy_followup") @db.VarChar(20)
  chemotherapy_cycle_id String             @db.VarChar(20)
  followup_date         DateTime           @db.Date
  next_followup_date    DateTime?          @db.Date
  treatment_response    String?            @db.VarChar(100)
  recist_response       String?            @db.VarChar(50)
  disease_progression   Boolean?           @default(false)
  progression_date      DateTime?          @db.Date
  progression_details   String?
  remission_status      String?            @db.VarChar(100)
  recurrence            Boolean?           @default(false)
  recurrence_date       DateTime?          @db.Date
  recurrence_site       String?            @db.VarChar(200)
  metastasis            Boolean?           @default(false)
  metastasis_site       String?            @db.VarChar(200)
  survival_status       String?            @db.VarChar(100)
  performance_status    String?            @db.VarChar(50)
  ongoing_symptoms      String?
  late_toxicity         String?
  supportive_care       String?
  followup_notes        String?
  physician_assessment  String?
  physician_id          String?            @db.VarChar(20)
  active_status         Int?               @default(1) @db.SmallInt
  deleted_flag          Boolean?           @default(false)
  created_by            String?            @db.VarChar(20)
  created_at            DateTime?          @default(now()) @db.Timestamp(6)
  updated_by            String?            @db.VarChar(20)
  updated_at            DateTime?          @default(now()) @db.Timestamp(6)
  chemotherapy_cycle    chemotherapy_cycle @relation(fields: [chemotherapy_cycle_id], references: [chemotherapy_cycle_id], onDelete: Cascade, map: "fk_chemo_followup_cycle")
  employees             employees?         @relation(fields: [physician_id], references: [employee_id], map: "fk_chemo_followup_doctor")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model chemotherapy_lab_review {
  id                    BigInt             @id @default(autoincrement())
  lab_review_id         String             @unique(map: "uq_chemo_lab_review") @db.VarChar(20)
  chemotherapy_cycle_id String             @db.VarChar(20)
  review_date           DateTime           @default(now()) @db.Timestamp(6)
  hemoglobin            Decimal?           @db.Decimal(5, 2)
  rbc                   Decimal?           @db.Decimal(10, 2)
  wbc                   Decimal?           @db.Decimal(10, 2)
  platelet_count        Decimal?           @db.Decimal(10, 2)
  neutrophil_count      Decimal?           @db.Decimal(10, 2)
  anc                   Decimal?           @db.Decimal(10, 2)
  creatinine            Decimal?           @db.Decimal(6, 2)
  creatinine_clearance  Decimal?           @db.Decimal(6, 2)
  blood_urea            Decimal?           @db.Decimal(6, 2)
  sgot_ast              Decimal?           @db.Decimal(6, 2)
  sgpt_alt              Decimal?           @db.Decimal(6, 2)
  bilirubin             Decimal?           @db.Decimal(6, 2)
  alkaline_phosphatase  Decimal?           @db.Decimal(6, 2)
  albumin               Decimal?           @db.Decimal(6, 2)
  sodium                Decimal?           @db.Decimal(6, 2)
  potassium             Decimal?           @db.Decimal(6, 2)
  calcium               Decimal?           @db.Decimal(6, 2)
  magnesium             Decimal?           @db.Decimal(6, 2)
  chloride              Decimal?           @db.Decimal(6, 2)
  phosphorus            Decimal?           @db.Decimal(6, 2)
  uric_acid             Decimal?           @db.Decimal(6, 2)
  coagulation_profile   String?
  urine_test_result     String?
  pregnancy_test        String?            @db.VarChar(50)
  cbc_normal            Boolean?           @default(true)
  renal_function_ok     Boolean?           @default(true)
  liver_function_ok     Boolean?           @default(true)
  chemotherapy_fit      Boolean?           @default(true)
  reviewed_by           String?            @db.VarChar(20)
  review_notes          String?
  active_status         Int?               @default(1) @db.SmallInt
  deleted_flag          Boolean?           @default(false)
  created_by            String?            @db.VarChar(20)
  created_at            DateTime?          @default(now()) @db.Timestamp(6)
  updated_by            String?            @db.VarChar(20)
  updated_at            DateTime?          @default(now()) @db.Timestamp(6)
  chemotherapy_cycle    chemotherapy_cycle @relation(fields: [chemotherapy_cycle_id], references: [chemotherapy_cycle_id], onDelete: Cascade, map: "fk_chemo_lab_cycle")
  employees             employees?         @relation(fields: [reviewed_by], references: [employee_id], map: "fk_chemo_lab_reviewed_by")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model chemotherapy_master {
  id                 BigInt             @id @default(autoincrement())
  chemo_id           String             @unique @db.VarChar(20)
  department_id      String?            @db.VarChar(20)
  branch_id          String?            @db.VarChar(20)
  total_cycles       Int?
  treatment_end_date DateTime?          @db.Date
  patient_history_id String?            @db.VarChar(20)
  created_by         String?            @db.VarChar(20)
  created_at         DateTime?          @default(now()) @db.Timestamp(6)
  diagnosis_id       String?            @db.VarChar(20)
  user_id            String?            @db.VarChar
  regimen_code       String?            @db.VarChar(50)
  protocol_version   String?            @db.VarChar(30)
  cancer_type        String?            @db.VarChar(100)
  cancer_stage       String?            @db.VarChar(50)
  active_status      Int?               @default(1) @db.SmallInt
  remarks            String?
  approved_by        String?            @db.VarChar(20)
  approved_at        DateTime?          @db.Timestamp(6)
  updated_by         String?            @db.VarChar(20)
  updated_at         DateTime?          @db.Timestamp(6)
  deleted_flag       Boolean?           @default(false)
  diagnosis          diagnosis?         @relation(fields: [diagnosis_id], references: [diagnosis_id], onDelete: Cascade)
  branch             branch?            @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_chemo_branch")
  department_master  department_master? @relation(fields: [department_id], references: [department_id], onDelete: Cascade, map: "fk_chemo_department")
  patient_history    patient_history?   @relation(fields: [patient_history_id], references: [patient_history_id], onDelete: Cascade, map: "fk_chemo_patient_history")
  user_table         user_table?        @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_chemo_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model chemotherapy_plan {
  id                      BigInt                    @id @default(autoincrement())
  chemotherapy_plan_id    String                    @unique(map: "uq_chemotherapy_plan") @db.VarChar(20)
  patient_history_id      String                    @db.VarChar(20)
  patient_id              String                    @db.VarChar(20)
  encounter_no            String?                   @db.VarChar(30)
  appointment_id          String?                   @db.VarChar(20)
  diagnosis_id            String                    @db.VarChar(20)
  employee_id             String                    @db.VarChar(20)
  department_id           String                    @db.VarChar(20)
  branch_id               String                    @db.VarChar(20)
  user_id                 String?                   @db.VarChar(20)
  regimen_name            String                    @db.VarChar(200)
  regimen_code            String?                   @db.VarChar(50)
  protocol_name           String?                   @db.VarChar(200)
  protocol_version        String?                   @db.VarChar(50)
  treatment_goal          String?                   @db.VarChar(50)
  treatment_intent        String?                   @db.VarChar(50)
  cancer_stage            String?                   @db.VarChar(50)
  cancer_type             String?                   @db.VarChar(150)
  cancer_subtype          String?                   @db.VarChar(150)
  ecog_status             String?                   @db.VarChar(20)
  karnofsky_score         Int?
  planned_cycles          Int
  completed_cycles        Int?                      @default(0)
  cycle_interval_days     Int?
  treatment_start_date    DateTime                  @db.Date
  expected_end_date       DateTime?                 @db.Date
  treatment_status        String?                   @default("PLANNED") @db.VarChar(30)
  consent_taken           Boolean?                  @default(false)
  consent_date            DateTime?                 @db.Date
  insurance_type          String?                   @db.VarChar(100)
  remarks                 String?
  active_status           Int?                      @default(1) @db.SmallInt
  deleted_flag            Boolean?                  @default(false)
  created_by              String?                   @db.VarChar(20)
  created_at              DateTime?                 @default(now()) @db.Timestamp(6)
  updated_by              String?                   @db.VarChar(20)
  updated_at              DateTime?                 @default(now()) @db.Timestamp(6)
  chemotherapy_cycle      chemotherapy_cycle[]
  appointment_history     appointment_history?      @relation(fields: [appointment_id], references: [appointment_id], map: "fk_chemo_plan_appointment")
  branch                  branch                    @relation(fields: [branch_id], references: [branch_id], map: "fk_chemo_plan_branch")
  department_master       department_master         @relation(fields: [department_id], references: [department_id], map: "fk_chemo_plan_department")
  diagnosis               diagnosis                 @relation(fields: [diagnosis_id], references: [diagnosis_id], map: "fk_chemo_plan_diagnosis")
  employees               employees                 @relation(fields: [employee_id], references: [employee_id], map: "fk_chemo_plan_employee")
  encounter               encounter?                @relation(fields: [encounter_no], references: [encounter_no], map: "fk_chemo_plan_encounter")
  patient_bio_data        patient_bio_data          @relation(fields: [patient_id], references: [patient_id], map: "fk_chemo_plan_patient")
  patient_history         patient_history           @relation(fields: [patient_history_id], references: [patient_history_id], map: "fk_chemo_plan_patient_history")
  user_table              user_table?               @relation(fields: [user_id], references: [user_id], map: "fk_chemo_plan_user")
  chemotherapy_plan_items chemotherapy_plan_items[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model chemotherapy_plan_items {
  id                          BigInt                        @id @default(autoincrement())
  chemotherapy_plan_item_id   String                        @unique(map: "uq_chemotherapy_plan_item") @db.VarChar(20)
  chemotherapy_plan_id        String                        @db.VarChar(20)
  medicine_id                 String                        @db.VarChar(20)
  drug_sequence               Int
  drug_type                   String?                       @db.VarChar(50)
  dosage                      Decimal?                      @db.Decimal(10, 2)
  dosage_unit                 String?                       @db.VarChar(30)
  dose_calculation_method     String?                       @db.VarChar(50)
  calculated_dose             Decimal?                      @db.Decimal(10, 2)
  administration_route        String?                       @db.VarChar(50)
  formulation                 String?                       @db.VarChar(100)
  infusion_type               String?                       @db.VarChar(50)
  infusion_duration_minutes   Int?
  infusion_rate               String?                       @db.VarChar(100)
  dilution_solution           String?                       @db.VarChar(100)
  dilution_volume             String?                       @db.VarChar(50)
  administration_day          Int?
  cycle_day                   Int?
  frequency                   String?                       @db.VarChar(100)
  maximum_dose                Decimal?                      @db.Decimal(10, 2)
  minimum_dose                Decimal?                      @db.Decimal(10, 2)
  dose_required               Boolean?                      @default(true)
  remarks                     String?
  active_status               Int?                          @default(1) @db.SmallInt
  created_by                  String?                       @db.VarChar(20)
  created_at                  DateTime?                     @default(now()) @db.Timestamp(6)
  updated_by                  String?                       @db.VarChar(20)
  updated_at                  DateTime?                     @default(now()) @db.Timestamp(6)
  chemotherapy_administration chemotherapy_administration[]
  medicine_master             medicine_master               @relation(fields: [medicine_id], references: [medicine_id], map: "fk_chemo_plan_items_medicine")
  chemotherapy_plan           chemotherapy_plan             @relation(fields: [chemotherapy_plan_id], references: [chemotherapy_plan_id], onDelete: Cascade, map: "fk_chemo_plan_items_plan")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model chemotherapy_vitals {
  id                       BigInt             @id @default(autoincrement())
  vital_id                 String             @unique(map: "uq_chemo_vital_id") @db.VarChar(20)
  chemotherapy_cycle_id    String             @db.VarChar(20)
  recorded_at              DateTime           @default(now()) @db.Timestamp(6)
  vital_stage              String?            @db.VarChar(30)
  blood_pressure_systolic  Int?
  blood_pressure_diastolic Int?
  pulse_rate               Int?
  respiratory_rate         Int?
  body_temperature         Decimal?           @db.Decimal(4, 1)
  spo2                     Int?
  height                   Decimal?           @db.Decimal(5, 2)
  weight                   Decimal?           @db.Decimal(5, 2)
  body_surface_area        Decimal?           @db.Decimal(5, 2)
  bmi                      Decimal?           @db.Decimal(5, 2)
  pain_score               Int?
  pain_location            String?            @db.VarChar(100)
  blood_sugar              Decimal?           @db.Decimal(6, 2)
  oxygen_support           Boolean?           @default(false)
  oxygen_flow_rate         Decimal?           @db.Decimal(5, 2)
  consciousness_level      String?            @db.VarChar(50)
  hydration_status         String?            @db.VarChar(50)
  recorded_by              String?            @db.VarChar(20)
  remarks                  String?
  active_status            Int?               @default(1) @db.SmallInt
  deleted_flag             Boolean?           @default(false)
  created_by               String?            @db.VarChar(20)
  created_at               DateTime?          @default(now()) @db.Timestamp(6)
  updated_by               String?            @db.VarChar(20)
  updated_at               DateTime?          @default(now()) @db.Timestamp(6)
  chemotherapy_cycle       chemotherapy_cycle @relation(fields: [chemotherapy_cycle_id], references: [chemotherapy_cycle_id], onDelete: Cascade, map: "fk_chemo_vitals_cycle")
  employees                employees?         @relation(fields: [recorded_by], references: [employee_id], map: "fk_chemo_vitals_recorded_by")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model clinical_test_master {
  id           BigInt    @id @default(autoincrement())
  testid       String    @unique @db.VarChar(20)
  testcode     String    @unique @db.VarChar(20)
  testname     String    @db.VarChar(200)
  category     String?   @db.VarChar(100)
  department   String?   @db.VarChar(100)
  sampletype   String?   @db.VarChar(100)
  normalrange  String?   @db.VarChar(100)
  unit         String?   @db.VarChar(50)
  testcharge   Decimal?  @db.Decimal(10, 2)
  isactive     String?   @db.VarChar(20)
  createddate  DateTime? @default(now()) @db.Timestamp(6)
  modifieddate DateTime? @default(now()) @db.Timestamp(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model department_master {
  department_id       String                @id(map: "department_pkey") @db.VarChar(20)
  department_name     String                @db.VarChar(100)
  appointment_history appointment_history[]
  chemotherapy_master chemotherapy_master[]
  chemotherapy_plan   chemotherapy_plan[]
  employees           employees[]
  encounter           encounter[]
  lab_history         lab_history[]
  lab_order           lab_order[]
  patient_history     patient_history[]
  prescription        prescription[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model diagnosis {
  id                         BigInt                       @id @default(autoincrement())
  diagnosis_id               String                       @unique @db.VarChar(20)
  diagnosis_name             String                       @db.VarChar(200)
  icd_code                   String?                      @db.VarChar(20)
  icd_version                String?                      @db.VarChar(20)
  diagnosis_category         String?                      @db.VarChar(100)
  diagnosis_description      String?
  diagnosis_alias            String?                      @db.VarChar(200)
  disease_group              String?                      @db.VarChar(100)
  body_system                String?                      @db.VarChar(100)
  body_site                  String?                      @db.VarChar(100)
  severity_level             String?                      @db.VarChar(30)
  is_chronic                 Boolean?                     @default(false)
  is_contagious              Boolean?                     @default(false)
  is_notifiable              Boolean?                     @default(false)
  standard_treatment         String?
  treatment_guidelines       String?
  followup_required          Boolean?                     @default(false)
  followup_days              Int?
  active_status              Int?                         @default(1) @db.SmallInt
  created_by                 String?                      @db.VarChar(20)
  created_at                 DateTime?                    @default(now()) @db.Timestamp(6)
  diagnosis_catogory_id      String?                      @db.VarChar
  chemotherapy_master        chemotherapy_master[]
  chemotherapy_plan          chemotherapy_plan[]
  diagnosis_medicine_mapping diagnosis_medicine_mapping[]
  patient_history            patient_history[]
  prescription               prescription[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model diagnosis_medicine_mapping {
  id              BigInt          @id @default(autoincrement())
  mapping_id      String          @unique @db.VarChar(20)
  diagnosis_id    String          @db.VarChar(20)
  medicine_id     String          @db.VarChar(20)
  is_primary      Boolean?        @default(false)
  dosage          String?         @db.VarChar(100)
  frequency       String?         @db.VarChar(100)
  duration        String?         @db.VarChar(50)
  instructions    String?
  created_at      DateTime?       @default(now()) @db.Timestamp(6)
  diagnosis       diagnosis       @relation(fields: [diagnosis_id], references: [diagnosis_id], onDelete: Cascade, map: "fk_dm_diagnosis")
  medicine_master medicine_master @relation(fields: [medicine_id], references: [medicine_id], onDelete: Cascade, map: "fk_dm_medicine")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model doctor_profile {
  employee_id          String    @unique @db.VarChar
  specialization       String?   @db.VarChar(100)
  qualification        String?   @db.VarChar(150)
  license_no           String?   @unique @db.VarChar(50)
  consultation_minutes Int       @default(15)
  id                   BigInt    @id @default(autoincrement())
  employees            employees @relation(fields: [employee_id], references: [employee_id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model doctor_schedule {
  schedule_id          BigInt                @id @default(autoincrement())
  employee_id          String                @db.VarChar
  branch_id            String                @db.VarChar(10)
  created_at           DateTime              @default(now()) @db.Timestamptz(6)
  day_of_week          String?               @db.VarChar(15)
  shift_name           String?               @db.VarChar(50)
  start_time           DateTime?             @db.Time(6)
  end_time             DateTime?             @db.Time(6)
  consultation_minutes Int?                  @default(20)
  is_active            Boolean?              @default(true)
  effective_from       DateTime?             @db.Timestamptz(6)
  effective_to         DateTime?             @db.Timestamptz(6)
  appointment_history  appointment_history[]
  employees            employees             @relation(fields: [employee_id], references: [employee_id], onDelete: NoAction, onUpdate: NoAction)
  branch               branch                @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_doctor_schedule_branch")
  encounter            encounter[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model employees {
  id                                                                                 BigInt                        @id @default(autoincrement())
  user_id                                                                            String?                       @unique(map: "uq_employees_user_id") @db.VarChar(20)
  first_name                                                                         String                        @db.VarChar(50)
  middle_name                                                                        String?                       @db.VarChar(50)
  last_name                                                                          String                        @db.VarChar(50)
  blood_group                                                                        String?                       @db.VarChar(5)
  nationality                                                                        String?                       @db.VarChar(50)
  marital_status                                                                     String?                       @db.VarChar(20)
  mobile_no                                                                          String?                       @db.VarChar(15)
  parmanent_address                                                                  String?                       @db.VarChar(255)
  current_address                                                                    String?                       @db.VarChar(255)
  emergency_contact_name                                                             String?                       @db.VarChar(100)
  emergency_contact_relationship                                                     String?                       @db.VarChar(50)
  emergency_contact_number                                                           String?                       @db.VarChar(15)
  aadhaar_no                                                                         String?                       @db.VarChar(20)
  pan_no                                                                             String?                       @db.VarChar(20)
  passport_no                                                                        String?                       @db.VarChar(20)
  designation                                                                        String?                       @db.VarChar(100)
  specialization                                                                     String?                       @db.VarChar(100)
  qualification                                                                      String?                       @db.VarChar(255)
  license_no                                                                         String?                       @db.VarChar(100)
  joining_date                                                                       DateTime?                     @db.Date
  branch_id                                                                          String?                       @db.VarChar(10)
  emp_status                                                                         Boolean?
  email                                                                              String?                       @db.VarChar(50)
  department_id                                                                      String?                       @db.VarChar(20)
  employee_id                                                                        String?                       @unique(map: "uq_employee_id") @db.VarChar(20)
  employee_photo_URL                                                                 String?                       @db.VarChar
  employee_state                                                                     String?                       @db.VarChar
  employee_district                                                                  String?                       @db.VarChar
  employee_area                                                                      String?                       @db.VarChar
  employee_pincode                                                                   BigInt?
  employee_no_experence                                                              BigInt?
  emp_gender                                                                         String?                       @db.VarChar
  emp_DOB                                                                            String?                       @db.VarChar
  appointment_history                                                                appointment_history[]
  chemotherapy_administration_chemotherapy_administration_verified_byToemployees     chemotherapy_administration[] @relation("chemotherapy_administration_verified_byToemployees")
  chemotherapy_administration_chemotherapy_administration_administered_byToemployees chemotherapy_administration[] @relation("chemotherapy_administration_administered_byToemployees")
  chemotherapy_adverse_event_chemotherapy_adverse_event_physician_idToemployees      chemotherapy_adverse_event[]  @relation("chemotherapy_adverse_event_physician_idToemployees")
  chemotherapy_adverse_event_chemotherapy_adverse_event_reported_byToemployees       chemotherapy_adverse_event[]  @relation("chemotherapy_adverse_event_reported_byToemployees")
  chemotherapy_followup                                                              chemotherapy_followup[]
  chemotherapy_lab_review                                                            chemotherapy_lab_review[]
  chemotherapy_plan                                                                  chemotherapy_plan[]
  chemotherapy_vitals                                                                chemotherapy_vitals[]
  doctor_profile                                                                     doctor_profile?
  doctor_schedule                                                                    doctor_schedule[]
  branch                                                                             branch?                       @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_employee_branch")
  department_master                                                                  department_master?            @relation(fields: [department_id], references: [department_id], onDelete: Cascade, map: "fk_employee_department")
  user_table                                                                         user_table?                   @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_employee_user")
  encounter                                                                          encounter[]
  lab_history                                                                        lab_history[]
  lab_order                                                                          lab_order[]
  lab_report                                                                         lab_report[]
  lab_result                                                                         lab_result[]
  patient_history                                                                    patient_history[]
  prescription                                                                       prescription[]
  sample_collection                                                                  sample_collection[]
  user_branch_mapping                                                                user_branch_mapping[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model encounter {
  encounter_id        BigInt               @id @default(autoincrement())
  encounter_no        String               @unique @db.VarChar(30)
  branch_id           String               @db.VarChar(10)
  encounter_ts        DateTime             @default(now()) @db.Timestamptz(6)
  encounter_type      String               @default("OPD") @db.VarChar(20)
  status              String               @default("OPEN") @db.VarChar(20)
  notes               String?
  created_at          DateTime             @default(now()) @db.Timestamptz(6)
  employee_id         String?              @db.VarChar(20)
  patient_id          String               @db.VarChar(20)
  appointment_id      String?              @unique @db.VarChar(20)
  advice              String?
  checkout_time       DateTime?            @db.Timestamptz(6)
  chief_complaint     String?
  clinical_notes      String?
  closed_at           DateTime?            @db.Timestamptz(6)
  closed_by           String?              @db.VarChar(20)
  department_id       String?              @db.VarChar(20)
  diastolic_bp        Int?
  follow_up_date      DateTime?            @db.Date
  height              Decimal?             @db.Decimal(5, 2)
  pulse               Int?
  respiratory_rate    Int?
  schedule_id         BigInt?
  spo2                Int?
  symptoms            String?
  systolic_bp         Int?
  temperature         Decimal?             @db.Decimal(5, 2)
  weight              Decimal?             @db.Decimal(5, 2)
  user_id             String?              @db.VarChar(20)
  diagnosis_id        String?              @db.VarChar(20)
  diagnosis_text      String?              @db.VarChar
  chemotherapy_plan   chemotherapy_plan[]
  appointment_history appointment_history? @relation(fields: [appointment_id], references: [appointment_id], onDelete: Cascade, map: "fk_encounter_appointment")
  branch              branch               @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_encounter_branch")
  department_master   department_master?   @relation(fields: [department_id], references: [department_id], onDelete: Cascade, map: "fk_encounter_department")
  employees           employees?           @relation(fields: [employee_id], references: [employee_id], onDelete: NoAction, map: "fk_encounter_doctor")
  patient_bio_data    patient_bio_data     @relation(fields: [patient_id], references: [patient_id], onDelete: Cascade, map: "fk_encounter_patient")
  doctor_schedule     doctor_schedule?     @relation(fields: [schedule_id], references: [schedule_id], onDelete: NoAction, map: "fk_encounter_schedule")
  user_table          user_table?          @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_encounter_user")

  @@index([patient_id], map: "idx_encounter_patient")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model hospital {
  hospital_id         String   @unique(map: "hospital_id_unique") @db.VarChar(20)
  hospital_name       String   @db.VarChar(200)
  Hospital_address    String?  @db.VarChar(300)
  pincode             String?  @db.VarChar(10)
  contact_number      String?  @db.VarChar(15)
  fax_number          String?  @db.VarChar(20)
  email_id            String?  @db.VarChar(150)
  website_address     String?  @db.VarChar(200)
  hospital_type       String?  @db.VarChar(50)
  hospital_license_no String?  @db.VarChar(50)
  id                  BigInt   @id @default(autoincrement())
  branch              branch[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model id_sequences {
  entity_name    String    @id @db.VarChar(50)
  prefix         String    @db.VarChar(10)
  current_number Int       @default(0)
  updated_at     DateTime? @default(now()) @db.Timestamp(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model lab_history {
  id                           BigInt               @id @default(autoincrement())
  lab_history_id               String?              @unique @db.VarChar(20)
  patient_history_id           String?              @db.VarChar(20)
  requested_id                 String?              @db.VarChar(20)
  patient_full_name            String?              @db.VarChar(200)
  reason_for_exclusion         String?
  collection_on                DateTime?            @db.Timestamp(6)
  requisition_no               String?              @db.VarChar(50)
  lab_technician_name          String?              @db.VarChar(150)
  collection_remarks           String?
  qr_code                      String?              @db.VarChar(255)
  sample_id                    String?              @db.VarChar(50)
  received_datetime            DateTime?            @db.Timestamp(6)
  received_by                  String?              @db.VarChar(150)
  sample_verification_comments String?
  quality_action               String?              @db.VarChar(200)
  quality_completed_date       DateTime?            @db.Timestamp(6)
  quality_completed_by         String?              @db.VarChar(150)
  quality_check_comment        String?
  completed_on                 DateTime?            @db.Timestamp(6)
  clinical_correlation         String?
  approve_result_remarks       String?
  sent_on                      DateTime?            @db.Timestamp(6)
  temperature                  String?              @db.VarChar(50)
  transport_medium             String?              @db.VarChar(100)
  container_sealed             Boolean?
  transport_time               DateTime?            @db.Timestamp(6)
  appointment_id               String?              @db.VarChar(20)
  branch_id                    String?              @db.VarChar(10)
  department_id                String?              @db.VarChar(20)
  user_id                      String?              @db.VarChar(20)
  diagnosis_id                 String?              @db.VarChar(20)
  employee_id                  String?              @db.VarChar(20)
  lab_order_id                 String               @db.VarChar(20)
  lab_order_item_id            String               @db.VarChar(20)
  sample_collection_id         String               @db.VarChar(20)
  lab_result_id                String               @db.VarChar(20)
  lab_report_id                String               @db.VarChar(20)
  appointment_history          appointment_history? @relation(fields: [appointment_id], references: [appointment_id], onDelete: Cascade, map: "fk_lab_appointment")
  branch                       branch?              @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_lab_branch")
  department_master            department_master?   @relation(fields: [department_id], references: [department_id], onDelete: Cascade, map: "fk_lab_department")
  lab_order                    lab_order            @relation(fields: [lab_order_id], references: [lab_order_id], onDelete: Cascade, map: "fk_lab_history_order")
  lab_order_item               lab_order_item       @relation(fields: [lab_order_item_id], references: [lab_order_item_id], onDelete: Cascade, map: "fk_lab_history_order_item")
  lab_report                   lab_report           @relation(fields: [lab_report_id], references: [lab_report_id], onDelete: Cascade, map: "fk_lab_history_report")
  lab_result                   lab_result           @relation(fields: [lab_result_id], references: [lab_result_id], onDelete: Cascade, map: "fk_lab_history_result")
  sample_collection            sample_collection    @relation(fields: [sample_collection_id], references: [sample_collection_id], onDelete: Cascade, map: "fk_lab_history_sample")
  patient_history              patient_history?     @relation(fields: [patient_history_id], references: [patient_history_id], onDelete: Cascade, map: "fk_lab_patient_history")
  user_table                   user_table?          @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_lab_user")
  employees                    employees?           @relation(fields: [employee_id], references: [employee_id], onDelete: Cascade)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model lab_order {
  id                    BigInt               @id @default(autoincrement())
  lab_order_id          String               @unique @db.VarChar(20)
  patient_history_id    String               @db.VarChar(20)
  appointment_id        String?              @db.VarChar(20)
  doctor_employee_id    String               @db.VarChar(20)
  department_id         String?              @db.VarChar(20)
  order_datetime        DateTime?            @default(now()) @db.Timestamp(6)
  priority              String?              @default("Normal") @db.VarChar(20)
  clinical_notes        String?
  provisional_diagnosis String?
  order_status          String?              @default("Ordered") @db.VarChar(30)
  branch_id             String?              @db.VarChar(10)
  user_id               String?              @db.VarChar(20)
  created_at            DateTime?            @default(now()) @db.Timestamp(6)
  updated_at            DateTime?            @default(now()) @db.Timestamp(6)
  lab_history           lab_history[]
  appointment_history   appointment_history? @relation(fields: [appointment_id], references: [appointment_id], onDelete: Cascade, map: "fk_lab_order_appointment")
  branch                branch?              @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_lab_order_branch")
  department_master     department_master?   @relation(fields: [department_id], references: [department_id], onDelete: Restrict, map: "fk_lab_order_department")
  employees             employees            @relation(fields: [doctor_employee_id], references: [employee_id], map: "fk_lab_order_doctor")
  patient_history       patient_history      @relation(fields: [patient_history_id], references: [patient_history_id], onDelete: Cascade, map: "fk_lab_order_patient_history")
  user_table            user_table?          @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_lab_order_user")
  lab_order_item        lab_order_item[]
  lab_report            lab_report[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model lab_result {
  id                   BigInt            @id @default(autoincrement())
  lab_result_id        String            @unique @db.VarChar(20)
  sample_collection_id String            @db.VarChar(20)
  lab_order_item_id    String            @db.VarChar(20)
  parameter_name       String            @db.VarChar(200)
  result_value         String?           @db.VarChar(200)
  abnormal_flag        String?           @db.VarChar(20)
  result_comment       String?
  verified_by          String?           @db.VarChar(20)
  verified_datetime    DateTime?         @db.Timestamp(6)
  result_status        String?           @default("Pending") @db.VarChar(30)
  branch_id            String?           @db.VarChar(10)
  user_id              String?           @db.VarChar(20)
  created_at           DateTime?         @default(now()) @db.Timestamp(6)
  updated_at           DateTime?         @default(now()) @db.Timestamp(6)
  lab_history          lab_history[]
  branch               branch?           @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_lab_result_branch")
  employees            employees?        @relation(fields: [verified_by], references: [employee_id], onDelete: Restrict, map: "fk_lab_result_employee")
  lab_order_item       lab_order_item    @relation(fields: [lab_order_item_id], references: [lab_order_item_id], onDelete: Cascade, map: "fk_lab_result_order_item")
  sample_collection    sample_collection @relation(fields: [sample_collection_id], references: [sample_collection_id], onDelete: Cascade, map: "fk_lab_result_sample")
  user_table           user_table?       @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_lab_result_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model login_otp {
  id          BigInt   @id @default(autoincrement())
  otp_id      String   @unique @db.VarChar(20)
  user_id     String   @db.VarChar(20)
  user_type   String   @db.VarChar(20)
  otp_code    String   @db.VarChar(6)
  is_verified Boolean  @default(false)
  expires_at  DateTime @db.Timestamp(6)
  created_at  DateTime @default(now()) @db.Timestamp(6)

  @@index([user_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model medicine_master {
  id                         BigInt                       @id @default(autoincrement())
  medicine_id                String                       @unique @db.VarChar(20)
  medicine_code              String?                      @unique @db.VarChar(30)
  barcode                    String?                      @db.VarChar(100)
  medicine_category          String?                      @db.VarChar(100)
  medicine_type              String?                      @db.VarChar(50)
  unit                       String?                      @db.VarChar(20)
  medicine_name              String                       @db.VarChar(200)
  generic_name               String?                      @db.VarChar(200)
  strength                   String?                      @db.VarChar(50)
  dosage_form                String?                      @db.VarChar(50)
  route                      String?                      @db.VarChar(50)
  pack_size                  String?                      @db.VarChar(50)
  manufacturer_name          String?                      @db.VarChar(200)
  supplier_name              String?                      @db.VarChar(200)
  hsn_code                   String?                      @db.VarChar(20)
  gst_percent                Decimal?                     @db.Decimal(5, 2)
  landing_cost               Decimal?                     @db.Decimal(10, 2)
  purchase_rate              Decimal?                     @db.Decimal(10, 2)
  mrp                        Decimal?                     @db.Decimal(10, 2)
  selling_price              Decimal?                     @db.Decimal(10, 2)
  schedule_type              String?                      @db.VarChar(20)
  prescription_required      Boolean?                     @default(true)
  is_narcotic                Boolean?                     @default(false)
  is_high_risk               Boolean?                     @default(false)
  is_batch_required          Boolean?                     @default(true)
  is_expiry_required         Boolean?                     @default(true)
  storage_condition          String?                      @db.VarChar(200)
  min_stock                  Int?                         @default(0)
  max_stock                  Int?                         @default(0)
  reorder_level              Int?                         @default(0)
  medicine_master_remarks    String?
  is_active                  Boolean?                     @default(true)
  chemotherapy_plan_items    chemotherapy_plan_items[]
  diagnosis_medicine_mapping diagnosis_medicine_mapping[]
  prescription_items         prescription_items[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model patient_bio_data {
  id                       BigInt                @id @default(autoincrement())
  patient_id               String                @unique @db.VarChar(20)
  user_id                  String?               @db.VarChar(20)
  branch_id                String?               @db.VarChar(20)
  patient_first_name       String                @db.VarChar(100)
  patient_middle_name      String?               @db.VarChar(100)
  patient_last_name        String?               @db.VarChar(100)
  patient_gender           String?               @db.VarChar(20)
  patient_dob              DateTime?             @db.Date
  patient_age              Int?
  patient_blood_group      String?               @db.VarChar(10)
  patient_primary_mobile   String?               @db.VarChar(20)
  patient_alternate_mobile String?               @db.VarChar(20)
  patient_email            String?               @db.VarChar(150)
  patient_marital_status   String?               @db.VarChar(30)
  patient_nationality      String?               @db.VarChar(100)
  patient_photo_url        String?
  patient_type             String?               @db.VarChar(50)
  patient_active           String?               @db.VarChar(20)
  patient_state            String?               @db.VarChar
  patient_district         String?               @db.VarChar
  patient_area             String?               @db.VarChar
  patient_pincode          BigInt?
  appointment_history      appointment_history[]
  chemotherapy_plan        chemotherapy_plan[]
  encounter                encounter[]
  branch                   branch?               @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_patient_bio_branch")
  user_table               user_table?           @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_patient_bio_user")
  patient_history          patient_history[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model patient_history {
  id                         BigInt                @id @default(autoincrement())
  mrn_no                     String?               @db.VarChar(30)
  previous_hospital_name     String?               @db.VarChar(200)
  previous_doctor_name       String?               @db.VarChar(200)
  previous_visit_date        DateTime?             @db.Date
  previous_diagnosis         String?
  previous_treatment         String?
  previous_surgery           String?
  previous_medications       String?
  previous_hospitalization   String?
  allergy_history            String?
  chronic_disease            String?
  visit_type                 String?               @db.VarChar(30)
  visit_date                 DateTime?             @db.Date
  history_of_present_illness String?
  clinical_notes             String?
  height                     Decimal?              @db.Decimal(5, 2)
  weight                     Decimal?              @db.Decimal(5, 2)
  bmi                        Decimal?              @db.Decimal(5, 2)
  temperature                Decimal?              @db.Decimal(5, 2)
  pulse                      Int?
  respiratory_rate           Int?
  systolic_bp                Int?
  diastolic_bp               Int?
  oxygen_saturation          Decimal?              @db.Decimal(5, 2)
  blood_sugar                String?               @db.VarChar(50)
  severity                   String?               @db.VarChar(30)
  treatment_plan             String?
  procedure_done             String?
  surgery_done               String?
  operation_notes            String?
  physiotherapy_notes        String?
  lab_summary                String?
  scan_summary               String?
  admitted                   String?               @db.VarChar(50)
  admission_date             DateTime?             @db.Date
  discharge_date             DateTime?             @db.Date
  ward                       String?               @db.VarChar(100)
  room                       String?               @db.VarChar(50)
  bed                        String?               @db.VarChar(50)
  discharge_summary          String?
  treating_doctor_name       String?               @db.VarChar(150)
  consulting_doctor_name     String?               @db.VarChar(150)
  uploaded_document_url      String?
  visit_status               String?               @db.VarChar(30)
  updated_by                 String?               @db.VarChar(20)
  updated_at                 DateTime?             @default(now()) @db.Timestamp(6)
  appointment_id             String?               @db.VarChar(20)
  user_id                    String?               @db.VarChar(20)
  branch_id                  String?               @db.VarChar(20)
  department_id              String?               @db.VarChar(20)
  patient_history_id         String                @unique @db.VarChar(20)
  patient_id                 String                @db.VarChar
  diagnosis_id               String?               @db.VarChar(20)
  employee_id                String?               @db.VarChar(20)
  dosage_details             String?
  duration                   String?
  medicine_instruction       String?
  prescribed_medicines       String?
  chemotherapy_master        chemotherapy_master[]
  chemotherapy_plan          chemotherapy_plan[]
  lab_history                lab_history[]
  lab_order                  lab_order[]
  diagnosis                  diagnosis?            @relation(fields: [diagnosis_id], references: [diagnosis_id], onDelete: Cascade, map: "fk_patient_history_diagnosis")
  patient_bio_data           patient_bio_data      @relation(fields: [patient_id], references: [patient_id], onDelete: Cascade, map: "fk_patient_history_patient")
  appointment_history        appointment_history?  @relation(fields: [appointment_id], references: [appointment_id], onDelete: Cascade, map: "fk_patienthistory_appointment")
  branch                     branch?               @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_patienthistory_branch")
  department_master          department_master?    @relation(fields: [department_id], references: [department_id], onDelete: Cascade, map: "fk_patienthistory_department")
  user_table                 user_table?           @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_patienthistory_user")
  employees                  employees?            @relation(fields: [employee_id], references: [employee_id])
  prescription               prescription[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model prescription {
  id                  BigInt               @id @default(autoincrement())
  prescription_id     String               @unique @db.VarChar(20)
  employee_id         String               @db.VarChar(20)
  department_id       String?              @db.VarChar(20)
  diagnosis_id        String?              @db.VarChar(20)
  prescription_date   DateTime?            @default(now()) @db.Timestamp(6)
  patient_history_id  String?              @db.VarChar(20)
  visit_type          String?              @db.VarChar(30)
  chief_complaint     String?
  clinical_notes      String?
  advice              String?
  followup_date       DateTime?            @db.Date
  prescription_status String?              @db.VarChar(30)
  branch_id           String?              @db.VarChar(20)
  user_id             String?              @db.VarChar(20)
  created_at          DateTime?            @default(now()) @db.Timestamp(6)
  branch              branch?              @relation(fields: [branch_id], references: [branch_id], onDelete: Restrict, map: "fk_prescription_branch")
  department_master   department_master?   @relation(fields: [department_id], references: [department_id], onDelete: Restrict, map: "fk_prescription_department")
  diagnosis           diagnosis?           @relation(fields: [diagnosis_id], references: [diagnosis_id], onDelete: Restrict, map: "fk_prescription_diagnosis")
  employees           employees            @relation(fields: [employee_id], references: [employee_id], map: "fk_prescription_doctor")
  user_table          user_table?          @relation(fields: [user_id], references: [user_id], onDelete: Restrict, map: "fk_prescription_user")
  patient_history     patient_history?     @relation(fields: [patient_history_id], references: [patient_history_id], onDelete: Cascade)
  prescription_items  prescription_items[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model prescription_items {
  id                   BigInt          @id @default(autoincrement())
  prescription_item_id String          @unique @db.VarChar(20)
  prescription_id      String          @db.VarChar(20)
  medicine_id          String          @db.VarChar(20)
  dosage               String?         @db.VarChar(100)
  frequency            String?         @db.VarChar(100)
  days                 Int?
  unit                 String?         @db.VarChar(20)
  duration             String?         @db.VarChar(50)
  quantity             Int?
  route                String?         @db.VarChar(50)
  before_after_food    String?         @db.VarChar(30)
  morning              Boolean?        @default(false)
  afternoon            Boolean?        @default(false)
  night                Boolean?        @default(false)
  instruction          String?
  medicine_master      medicine_master @relation(fields: [medicine_id], references: [medicine_id], map: "fk_prescription_items_medicine")
  prescription         prescription    @relation(fields: [prescription_id], references: [prescription_id], onDelete: Cascade, map: "fk_prescription_items_prescription")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model role_id_config {
  role_type      String    @id @db.VarChar(50)
  prefix         String    @unique @db.VarChar(5)
  current_number Int       @default(0)
  description    String?   @db.VarChar(100)
  created_at     DateTime? @default(now()) @db.Timestamp(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_branch_mapping {
  user_id           String     @db.VarChar(20)
  branch_id         String     @db.VarChar(10)
  assigned_date     DateTime?  @default(now()) @db.Timestamp(6)
  status            Int?       @default(1) @db.SmallInt
  id                BigInt     @id @default(autoincrement())
  mapping_id        String?    @db.VarChar(20)
  employee_id       String?    @db.VarChar(20)
  is_primary_branch Boolean?   @default(false)
  branch            branch     @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_mapping_branch")
  employees         employees? @relation(fields: [employee_id], references: [employee_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_mapping_employee")
  user_table        user_table @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_mapping_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_log {
  id          BigInt     @id @default(autoincrement())
  log_id      String     @unique(map: "uq_user_log_log_id") @db.VarChar(20)
  user_id     String     @db.VarChar(20)
  module_name String?    @db.VarChar(100)
  action_type String     @db.VarChar(50)
  action_time DateTime?  @default(now()) @db.Timestamp(6)
  status      String?    @db.VarChar(20)
  branch_id   String?    @db.VarChar(10)
  branch      branch?    @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_user_log_branch")
  user_table  user_table @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_user_log_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_table {
  user_id             String?               @unique(map: "user_table_user_id_unique") @db.VarChar(20)
  role_type           String?               @db.VarChar(50)
  created_by          String?               @db.VarChar(20)
  username            String?               @db.VarChar(50)
  password            String?               @db.VarChar(255)
  user_status         Int?                  @default(0) @db.SmallInt
  created_at          DateTime?             @default(now()) @db.Timestamp(6)
  id                  BigInt                @id @default(autoincrement())
  branch_id           String?               @db.VarChar(10)
  appointment_history appointment_history[]
  chemotherapy_master chemotherapy_master[]
  chemotherapy_plan   chemotherapy_plan[]
  employees           employees?
  encounter           encounter[]
  lab_history         lab_history[]
  lab_order           lab_order[]
  lab_order_item      lab_order_item[]
  lab_report          lab_report[]
  lab_result          lab_result[]
  lab_test_category   lab_test_category[]
  lab_test_master     lab_test_master[]
  patient_bio_data    patient_bio_data[]
  patient_history     patient_history[]
  prescription        prescription[]
  sample_collection   sample_collection[]
  user_branch_mapping user_branch_mapping[]
  user_log            user_log[]
  branch              branch?               @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_user_branch")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model lab_order_item {
  id                BigInt              @id @default(autoincrement())
  lab_order_item_id String              @unique @db.VarChar(20)
  lab_order_id      String              @db.VarChar(20)
  lab_test_id       String              @db.VarChar(20)
  quantity          Int?                @default(1)
  price             Decimal?            @default(0.00) @db.Decimal(10, 2)
  discount          Decimal?            @default(0.00) @db.Decimal(10, 2)
  net_amount        Decimal?            @default(0.00) @db.Decimal(10, 2)
  item_status       String?             @default("Ordered") @db.VarChar(30)
  remarks           String?
  created_at        DateTime?           @default(now()) @db.Timestamp(6)
  updated_at        DateTime?           @default(now()) @db.Timestamp(6)
  branch_id         String?             @db.VarChar(10)
  user_id           String?             @db.VarChar(20)
  lab_history       lab_history[]
  branch            branch?             @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_lab_order_item_branch")
  lab_order         lab_order           @relation(fields: [lab_order_id], references: [lab_order_id], onDelete: Cascade, map: "fk_lab_order_item_order")
  lab_test_master   lab_test_master     @relation(fields: [lab_test_id], references: [lab_test_id], map: "fk_lab_order_item_test")
  user_table        user_table?         @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_lab_order_item_user")
  lab_result        lab_result[]
  sample_collection sample_collection[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model lab_report {
  id                 BigInt        @id @default(autoincrement())
  lab_report_id      String        @unique @db.VarChar(20)
  lab_order_id       String        @db.VarChar(20)
  report_number      String?       @unique @db.VarChar(50)
  generated_datetime DateTime?     @default(now()) @db.Timestamp(6)
  approved_by        String?       @db.VarChar(20)
  approved_datetime  DateTime?     @db.Timestamp(6)
  report_status      String?       @default("Generated") @db.VarChar(30)
  report_file        String?
  digital_signature  String?
  report_comment     String?
  delivered_to       String?       @db.VarChar(150)
  delivered_datetime DateTime?     @db.Timestamp(6)
  branch_id          String?       @db.VarChar(10)
  user_id            String?       @db.VarChar(20)
  created_at         DateTime?     @default(now()) @db.Timestamp(6)
  updated_at         DateTime?     @default(now()) @db.Timestamp(6)
  lab_history        lab_history[]
  branch             branch?       @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_lab_report_branch")
  employees          employees?    @relation(fields: [approved_by], references: [employee_id], onDelete: Restrict, map: "fk_lab_report_employee")
  lab_order          lab_order     @relation(fields: [lab_order_id], references: [lab_order_id], onDelete: Cascade, map: "fk_lab_report_order")
  user_table         user_table?   @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_lab_report_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model lab_test_category {
  id                   BigInt            @id @default(autoincrement())
  lab_test_category_id String            @unique @db.VarChar(20)
  category_name        String            @unique @db.VarChar(100)
  category_code        String?           @unique @db.VarChar(20)
  description          String?
  display_order        Int?              @default(1)
  category_status      Int?              @default(1) @db.SmallInt
  created_at           DateTime?         @default(now()) @db.Timestamp(6)
  updated_at           DateTime?         @default(now()) @db.Timestamp(6)
  branch_id            String?           @db.VarChar(10)
  user_id              String?           @db.VarChar(20)
  branch               branch?           @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_lab_category_branch")
  user_table           user_table?       @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_lab_category_user")
  lab_test_master      lab_test_master[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model lab_test_master {
  id                   BigInt            @id @default(autoincrement())
  lab_test_id          String            @unique @db.VarChar(20)
  lab_test_category_id String            @db.VarChar(20)
  test_name            String            @db.VarChar(200)
  test_code            String            @unique @db.VarChar(50)
  sample_type          String?           @db.VarChar(100)
  required_volume      String?           @db.VarChar(50)
  unit                 String?           @db.VarChar(50)
  reference_range      String?           @db.VarChar(100)
  price                Decimal?          @db.Decimal(10, 2)
  tat_hours            Int?
  test_status          Int?              @default(1) @db.SmallInt
  branch_id            String?           @db.VarChar(10)
  user_id              String?           @db.VarChar(20)
  created_at           DateTime?         @default(now()) @db.Timestamp(6)
  updated_at           DateTime?         @default(now()) @db.Timestamp(6)
  lab_order_item       lab_order_item[]
  branch               branch?           @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_lab_test_branch")
  lab_test_category    lab_test_category @relation(fields: [lab_test_category_id], references: [lab_test_category_id], map: "fk_lab_test_category")
  user_table           user_table?       @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_lab_test_user")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sample_collection {
  id                   BigInt         @id @default(autoincrement())
  sample_collection_id String         @unique @db.VarChar(20)
  lab_order_item_id    String         @db.VarChar(20)
  barcode              String?        @unique @db.VarChar(100)
  container_type       String?        @db.VarChar(100)
  collection_datetime  DateTime?      @default(now()) @db.Timestamp(6)
  collected_by         String?        @db.VarChar(20)
  collection_site      String?        @db.VarChar(150)
  collected_volume     String?        @db.VarChar(50)
  collection_status    String?        @default("Collected") @db.VarChar(30)
  rejection_reason     String?
  remarks              String?
  branch_id            String?        @db.VarChar(10)
  user_id              String?        @db.VarChar(20)
  created_at           DateTime?      @default(now()) @db.Timestamp(6)
  updated_at           DateTime?      @default(now()) @db.Timestamp(6)
  lab_history          lab_history[]
  lab_result           lab_result[]
  branch               branch?        @relation(fields: [branch_id], references: [branch_id], onDelete: Cascade, map: "fk_sample_collection_branch")
  employees            employees?     @relation(fields: [collected_by], references: [employee_id], onDelete: Restrict, map: "fk_sample_collection_employee")
  lab_order_item       lab_order_item @relation(fields: [lab_order_item_id], references: [lab_order_item_id], onDelete: Cascade, map: "fk_sample_collection_order_item")
  user_table           user_table?    @relation(fields: [user_id], references: [user_id], onDelete: Cascade, map: "fk_sample_collection_user")
}
